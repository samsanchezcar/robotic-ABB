name: Generate Mermaid Diagrams

on:
  push:
    paths:
      - 'Diagrams/*.mmd'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Mermaid CLI
        run: npm install -g @mermaid-js/mermaid-cli

      - name: Create output directory
        run: mkdir -p Diagrams

      - name: Generate SVGs from .mmd files
        env:
          PUPPETEER_EXECUTABLE_PATH: "chromium"
          PUPPETEER_ARGS: "--no-sandbox"
        run: |
          for file in Diagrams/*.mmd; do
            filename=$(basename "$file" .mmd)
            echo "Generating SVG for $file..."
            mmdc -i "$file" -o "Diagrams/$filename.svg"
          done

      - name: Commit and push generated SVGs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Diagrams/*.svg || echo "No SVGs found"
          git commit -m "Auto-generate Mermaid SVGs [skip ci]" || echo "No changes to commit"
          git push || echo "No push needed"
